import CustomSpinner from "./CustomSpinner";

const TableSpinner = () => {
    return (
        <tr>
            <td colSpan={5}>
                <CustomSpinner />
            </td>
        </tr>
    );
};

export default TableSpinner;

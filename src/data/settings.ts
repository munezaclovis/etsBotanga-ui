const settings = {
    api_url: import.meta.env.VITE_API_URL,
    api_timeout: 5000,
    access_token_name: "access_token",
    theme_settings_name: "current_theme",
    current_user_name: "current_user",
};

export default settings;
